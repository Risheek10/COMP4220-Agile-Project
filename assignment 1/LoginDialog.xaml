<Window x:Class="ywBookStoreGUI.LoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ywBookStoreGUI"
        mc:Ignorable="d"
        Title="Login" Height="160" Width="320">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- User Name -->
        <Label Grid.Column="0" Grid.Row="0" Margin="2">User Name:</Label>
        <TextBox x:Name="nameTextBox" Grid.Column="1" Grid.Row="0" Margin="2" MinWidth="150"/>
        
        <!-- Password -->
        <Label Grid.Column="0" Grid.Row="1" Margin="2">Password:</Label>
        <PasswordBox x:Name="passwordTextBox" Grid.Column="1" Grid.Row="1" Margin="2"/>
        
        <!-- Login Buttons -->
        <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,5">
            <Button Name="okButton" Content="Login" Click="okButton_click" IsDefault="True" MinWidth="80" Margin="0,0,5,0"/>
            <Button Name="cancelButton" Click="cancelButton_click" IsCancel="True" MinWidth="80" Margin="5,0,0,0" Content="Cancel"/>
        </StackPanel>
        
        <!-- Sign Up Section -->
        <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,10">
            <TextBlock Text="Don't have an account?" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <Button Name="signUpButton" Content="Sign Up" Click="signUpButton_click" MinWidth="80" Background="LightBlue"/>
        </StackPanel>
    </Grid>
</Window>
